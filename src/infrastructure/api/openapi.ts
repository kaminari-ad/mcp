/**
 * GENERATED FILE — placeholder until `npm run gen:api-types` is run
 * against a reachable Kaminari Ad API.
 *
 * Source : (not yet generated)
 * Tool   : openapi-typescript
 * Refresh: `npm run gen:api-types`
 *
 * Once generated, this file holds the full `paths` / `components`
 * surface as TypeScript types. `HttpApiGateway` imports from here to
 * type its outbound requests and responses.
 *
 * Until then, callers can keep using the hand-written DTOs in
 * `src/domain/ports/api-gateway.ts`; this file exports an empty
 * placeholder so the module path resolves.
 */

/* eslint-disable */

export type paths = Record<string, never>;
export type components = { schemas: Record<string, never> };
